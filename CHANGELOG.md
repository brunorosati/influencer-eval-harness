# Changelog

Log every label change and every rerun here. A label edited after a model run
invalidates the run; delete results/ and rerun the grid.

## v1.0 (unreleased)
- Criteria frozen at commit `<hash>` on `<date>`
- 80 cases labeled, second annotator on 24, kappa `<x.xx>`
- Full grid run `<run_id>`; pricing verified 2026-09-01
